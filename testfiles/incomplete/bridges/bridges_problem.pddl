(define (problem bridges)
    (:domain Bridges)

    (:init (at_0_0))

    (:goal
     (and
      (at_0_0)
      (holding_treasure_1)
      (holding_treasure_2)
      (holding_treasure_3)
     )
    )
)